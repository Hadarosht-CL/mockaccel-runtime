# SPDX-License-Identifier: Apache-2.0
failure-threshold: warning
ignored:
  - DL3008  # Pin apt-get versions. Impractical on a moving Debian base.
  - DL3006  # Tag image versions. False positive: ${TARGETARCH} alias resolves to a named local stage, not a registry image.
