# CODEOWNERS — https://docs.gitlab.com/ee/user/project/codeowners/
#
# Lines match file paths (gitignore-style). The user(s) or group(s) listed
# after the pattern are auto-requested as reviewers on every MR that touches
# matching files. When `main` is a protected branch with "Require approval
# from code owners" enabled, an MR cannot merge until an owner approves.
#
# Solo project today, so one catch-all rule. As the codebase grows you can
# add more specific rules above the catch-all (more specific = wins).

* @hadarosht-dev
